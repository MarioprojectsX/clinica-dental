---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import CitaForm from "../../components/CitaForm.astro";
import { CONVERSION_LABELS } from '../../config/googleAds.js';

const alternates = {
  es: "/es/cita",
  ca: "/ca/cita",
  en: "/en/cita",
};
---

<BaseLayout
  description="Book your appointment at Dental Sueca Clinic. Your first evaluation is free."
  locale="en"
  {alternates}
>
  <Header locale="en" currentPath="/en/cita" {alternates} />

  <!-- ========================= HERO FORM ========================= -->
  <section
    class="cita-hero"
    style="--bg:url('/assets/pedir-hero.png')"
    aria-label="Appointment form"
  >
    <div class="cita-hero__overlay"></div>

    <div class="cita-hero__inner container">
      <div class="cita-hero__card">
        <CitaForm locale="en" />
      </div>
    </div>
  </section>

  <Footer locale="en" />
  
  <script is:inline define:vars={{ CONVERSION_LABELS }}>
    // Google Ads conversion tracking - Appointment page view
    if (typeof window.trackConversion !== 'undefined') {
      window.trackConversion(CONVERSION_LABELS.APPOINTMENT_PAGE_VIEW);
    }
  </script>
</BaseLayout>

<style is:global>
  .cita-hero .formc__subtitle::after {
    content: "Your journey to a healthy, radiant smile starts here.";
  }
</style>
